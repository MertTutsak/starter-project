<resources>

    <style name="AppTheme.Base.Night" parent="Theme.AppCompat">
        <item name="android:background">@android:color/background_dark</item>
        <item name="android:textColor">@android:color/primary_text_dark</item>
        <item name="subtitleTextColor">@android:color/primary_text_dark</item>
    </style>

    <style name="AppTheme.Base.Light" parent="Theme.AppCompat">
        <item name="android:background">@android:color/background_light</item>
        <item name="android:textColor">@android:color/primary_text_light</item>
        <item name="subtitleTextColor">@android:color/primary_text_light</item>
    </style>

    <style name="ProgressDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@drawable/bottomsheet_dialog_transperant_bg</item>
        <item name="android:background">@drawable/bottomsheet_dialog_transperant_bg</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottomsheet_dialog_transperant_bg</item>
    </style>
</resources>
